- name: ping playbook
  host: web
  tasks: list of servers
  - name: ping the servers
    ansible.builtin.ping:
  - name: install nginx
    ansible.builtin.package:
      name: nginx
      state: present
    ansible.builtin.service:
      name: nginx
      state: started



