- name: loops
  hosts: localhost
  become: yes
  tasks:
  # - name: print persons
  #   ansible.builtin.debug:
  #    msg: "Hello user {{item}}"
  #   loop:
  #     - gopi
  #     - raju
  #     - ramu
  #     - shannu
   - name: packages installation
     ansible.builtin.package:
        name: "installing {{item}}"
        state: present
     loop:
       - git
       - mysql
       - postfix
  


  